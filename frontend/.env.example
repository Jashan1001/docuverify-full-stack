# Frontend API base URL
# For local dev with Vite proxy, you can keep this unset.
# For deployed frontend calling a separate backend, set this explicitly.
VITE_API_BASE_URL=https://api.example.com/api
